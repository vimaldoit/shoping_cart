import 'package:flutter/material.dart';

class Appcolors {
  static const primaryColor = Colors.green;
  static const backgroundColor = Color(0xfFFFFFFF);
  static Color backgroundColor2 = Colors.grey.shade100;
  static const accentColor = Colors.green;
  static const textColor = Colors.black;
  static const hintColor = Color(0xff75818F);
  static const inputBoxColor = Color(0xffffffff);
}
